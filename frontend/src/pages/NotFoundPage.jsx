// Shows a friendly fallback screen when the user visits a route that does not exist.
import React from "react";

const NotFoundPage = () => {
  return <div>NotFoundPage</div>;
};

export default NotFoundPage;
